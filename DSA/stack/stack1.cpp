// it is a linear data structure in which insertion and deletion only allowed at the end calld the top of the stack
// when we define the stack as an abstract data type then we are only interested in know the stack operation from user pov
// this simpy mean we are not interested in knowing the details we only interested in what type of o/p we can perform
// stack is a LIFO data structure
// operations on the stack are push , pop , top , size , empty 
